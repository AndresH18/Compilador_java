package taller_2;

/**
 * <p>Enum with the elements for the symbol's table columns' headers.</p>
 */
public enum ColumnType {
    LEXEMA,
    LINE,
    COLUMN,
    TOKEN,
    ID_TOKEN,
    TYPE,
    TYPE1,
    TYPE2;
}
